{
  "$schema": "https://v2-9-4.turborepo.dev/schema.json",
  "ui": "stream",
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": true,
      "persistent": true
    },
    "lint": {},
    "clean": {
      "cache": true
    }
  }
}
